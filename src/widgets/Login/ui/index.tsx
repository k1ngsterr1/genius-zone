import { LoginForm } from "@widgets/form/ui/loginForm";

export const LoginScreen = () => {
  return (
    <div className="wrapper">
      <h1 className="main-heading">Войдите чтобы начать обучение</h1>
      <LoginForm />
    </div>
  );
};
